# Claude project instructions

Read `AGENTS.md`, then follow `docs/conversion-guide.md` for conversion sessions
and `docs/java-architecture.md` for Java implementation.

Use English for all new content and communication. Preserve supplied source files.
Ask for the conversion scope before generating business code unless the user has
already specified it in the current session. Keep decisions and outputs isolated
per session. These instructions also apply when this file is loaded without
automatic AGENTS.md support.
